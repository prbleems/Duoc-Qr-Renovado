import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Duoc-qr-renovado',
};

export default {
  webDir: 'www',
  // otras configuraciones
};